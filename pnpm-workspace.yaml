# Approve the native build scripts pnpm blocks by default (pnpm 11 `allowBuilds`).
# esbuild: Vite's transformer. unrs-resolver: backs eslint-import-resolver-typescript.
allowBuilds:
  esbuild: true
  unrs-resolver: true
